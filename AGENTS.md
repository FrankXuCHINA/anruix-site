# 项目开发规则

- 本项目只使用 pnpm。
- 不得混用 npm、yarn 或 bun。
- 修改前先阅读现有代码和项目结构。
- 修改后运行项目实际存在的检查与构建命令。
- 添加新的生产依赖前必须说明原因。
- 不得在代码中写入 SecretId、SecretKey、Token、密码或其他凭据。
- 不得修改当前项目目录以外的文件。
- 不得删除 AstroPaper 的核心博客功能。
- 自定义组件优先放在 `src/components/custom/`。
- 自定义样式应集中管理，避免在多个页面重复粘贴。
- 文章优先使用标准 Markdown，减少主题专属 MDX 组件。
- 不得自行配置腾讯云、EdgeOne、COS、DNS 或备案。
- 每次任务完成后汇报修改文件、执行命令、测试结果和遗留问题。
